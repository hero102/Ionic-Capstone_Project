import { MapToCategoriesPipe } from './map-to-categories-pipe';

describe('MapToCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new MapToCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
